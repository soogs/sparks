#' tucker
#'
#' calculate tucker congruence between 2 column vectors
#'
#' @param hi
#'
#' @return hi
#'
#' @examples
#' hi
#'
#' @export

tucker <- function(vectorA, vectorB){
  coef <- sum(vectorA * vectorB) / sqrt(sum(vectorA^2)*sum(vectorB^2))
  return (coef)
}
